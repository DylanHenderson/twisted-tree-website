---
title: Account Limits
description: Twist Stock Take usage limits
---

import { Card, CardGrid } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';
import { Aside } from '@astrojs/starlight/components';
import { Steps } from '@astrojs/starlight/components';
import { LinkCard } from '@astrojs/starlight/components';

* Data is stored for the last 100 stock takes. We recommend downloading and storing your reports if required.
* Each stock take can handle up to 10,000 unique products/variants (Note these are unique products, you can go well over 10,000 for total count). You can separate your stock takes out with filters to handle larger inventories.

